"""Reference baselines kept for the ablation table.

``starter_bm25`` is the organiser's weak BM25 starter, preserved byte-for-byte
from ``starter/agent.py`` at clone time so it stays comparable after the real
agent takes over that import path.
"""
